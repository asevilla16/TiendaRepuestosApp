import { RepuestosFilterPipe } from './repuestos-filter.pipe';

describe('RepuestosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RepuestosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
