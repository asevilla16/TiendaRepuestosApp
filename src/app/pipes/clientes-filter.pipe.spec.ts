import { ClientesFilterPipe } from './clientes-filter.pipe';

describe('ClientesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
